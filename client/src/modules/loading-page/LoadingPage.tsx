import * as React from 'react';

/**
 * Render a loading page if app is still loading.
 */
const LoadingPage = () => (
  <div className="loader">
    <div className="container">LOADING...</div>
  </div>
);
 
export default LoadingPage;